apiVersion: apps/v1
kind: Deployment
metadata:
  name: comin-admin-dashboard-webapp
  namespace: comin-dev
  labels:
    app: comin-admin-dashboard-webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comin-admin-dashboard-webapp
  template:
    metadata:
      labels:
        app: comin-admin-dashboard-webapp
    spec:
      containers:
      - name: comin-admin-dashboard-webapp
        image: ghcr.io/kaveeshag/comin-admin-dashboard-webapp:fdd72ecaf24ad293c21aa77b2fac151b18284f55
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        resources:
          limits:
            cpu: "1"
            memory: "512Mi"
          requests:
            cpu: "0.5"
            memory: "256Mi"